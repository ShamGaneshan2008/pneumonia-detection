# Pneumonia Detection using FastAI + ResNet50 + Flask

## Steps

1. Install requirements:
   pip install -r requirements.txt

2. Train model:
   cd notebooks
   python train.py

3. Run app:
   cd ../app
   python app.py

4. Open:
   http://127.0.0.1:5000